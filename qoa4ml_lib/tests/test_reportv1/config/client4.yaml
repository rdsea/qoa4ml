client:
  user_id: aaltoseas
  instance_id: ml_inference2
  stage_id: ml_inference_ensemble
  functionality: REST
  application_name: test
  role: ml
registration_url: http://localhost:5010/registration
connector:
  - name: amqp_connector
    connector_class: AMQP
    config:
      end_point: localhost
      exchange_name: test_qoa4ml
      exchange_type: topic
      out_routing_key: test.client4
